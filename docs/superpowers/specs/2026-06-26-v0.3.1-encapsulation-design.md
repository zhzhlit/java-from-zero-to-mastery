# v0.3.1 Encapsulation Design

## Goal

Continue the Java Core stage with one learning-ready encapsulation lesson backed by Java 21 examples and starter/solution practice.

## Scope

- Add `StudyGoal` to `examples/java-core` to demonstrate invariants, controlled mutation, query methods, and private helper methods.
- Extend `exercises/object-oriented` with a `StudyGoal` starter skeleton, reference solution, and JUnit tests.
- Publish `docs/zh-CN/guide/02-java-core/02-encapsulation.md`.
- Connect README, roadmap, previous lesson, and VitePress navigation.
- Bump Maven project versions to `0.3.1-SNAPSHOT`.

## Out of Scope

- Inheritance, polymorphism, interfaces, records, exceptions as a standalone topic, and collections.
- English documentation parity.
- A release tag or GitHub release.

## Validation

- `mvn -B -pl examples/java-core -am test`
- `mvn -B -pl exercises/object-oriented/starter -am compile`
- `mvn -B -pl exercises/object-oriented/solution -am test`
- Root Maven verify and documentation checks before release.
